<?php

namespace Tests\Unit;

use Tests\TestCase;

class ResidentConfirmationPageTest extends TestCase
{
    /**
     * A basic test example.
     *
     * @return void
     */

    // Confirmation page have Milestone
    public function testContainMilestone()
    {
        // $this->get('/schedule/confirm');
        // Retrieve Milestone information
        // Check if there is Milestone information
        $this->assertTrue(true);
    }
    // Confirmation page have Objective
    public function testContainObjective()
    {
        // Retrieve Objective information
        // Check if there is Objective information
        $this->assertTrue(true);
    }
}
