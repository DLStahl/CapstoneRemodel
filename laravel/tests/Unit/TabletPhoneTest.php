<?php

namespace Tests\Unit;

use Tests\TestCase;

class TabletPhoneTest extends TestCase
{
    // Resize the screen size and check if the navigation bar become a dropdown menu
    public function testResizeNavigationToDropDown()
    {
        $this->assertTrue(true);
    }

    // Resize the screen size and check if the dropdown menu items display on the entire navigation bar
    public function testResizeDropDownToNavigation()
    {
        $this->assertTrue(true);
    }

    // Resize the screen size by the dimension that the navigation bar become dropdown menu to see if it become dropdown menu
    public function testResizeDropNavigationToDropDownMin()
    {
        $this->assertTrue(true);
    }

    // Resize the screen size and check the the tile REMODEL have change the font size based on screen size
    public function testTile()
    {
        $this->assertTrue(true);
    }
}
